<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyt.server.mapper.sys.IOrganizationMapper">

    <resultMap id="BaseResultMap" type="com.hyt.server.entity.sys.Organization">
        <id column="id" jdbcType="VARCHAR" property="id"/>
        <result column="organization_name" jdbcType="VARCHAR" property="organizationName"/>
        <result column="code" jdbcType="VARCHAR" property="code"/>
        <result column="p_id" jdbcType="VARCHAR" property="pId"/>
        <result column="area_id" jdbcType="VARCHAR" property="areaId"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, organization_name, code, p_id, area_id, create_time
    </sql>

    <!-- 查询机构信息 -->
    <select id="findAll" resultMap="BaseResultMap">
        select
          <include refid="Base_Column_List"/>
        FROM organization o where 1=1
        <if test="id != null">and id = #{id}</if>
        <if test="organizationName != null">AND organization_name LIKE '${organizationName}%'</if>
        <if test="code != null">AND code = #{code}</if>
        <if test="pId != null">AND p_id = #{pId}</if>
        <if test="areaId != null">AND area_id = #{areaId}</if>
        <choose>
            <when test="sortName !=null and sortName!='id'">
                ORDER BY ${sortName} ${sortOrder}
            </when >
            <otherwise>
                ORDER BY create_time DESC
            </otherwise>
        </choose>
    </select>

</mapper>